# CLAUDE.md — POE2 Unofficial Launcher

Electron + React 19 + Vite 7 + TypeScript launcher for Path of Exile / PoE 2.
Layout: `src/main/` (Electron main), `src/renderer/` (React UI), `src/shared/` (shared types).

## ⚠️ Documentation status

`README.md`, `docs/README_KR.md` were rewritten on 2026-05-13 from the wiki analysis (below) and are current as of that date — they describe user-facing behavior, not architecture. `CHANGELOG.md` is auto-generated by release-please. `.agents/skills/*` predate the wiki and may be stale; verify against source. **The canonical, maintained project context is the wiki below — start there.**

## Project memory (LLM wiki)

Persistent project context lives at `~/project_llm_wiki/wiki/projects/poe2-launcher.md` (Obsidian vault, Korean). Read it at the start of any non-trivial work.

After a meaningful decision or analysis: dump a raw note to
`~/project_llm_wiki/raw/projects/poe2-launcher/<YYYY-MM-DD>-<topic>.md`, then in that wiki repo run `/ingest raw/projects/poe2-launcher/<file>.md` to compile it into the wiki page. The wiki update follows the INGEST operation in the wiki's CLAUDE.md (surgical edits, `[source: raw/...]` citations).

To work with the wiki visible as read context: `claude --add-dir ~/project_llm_wiki`.

## Commands

- `npm run dev` — Vite dev server (Electron). `chcp 65001` for UTF-8 on Windows.
- `npm run build` — `tsc && vite build && electron-builder`. `npm run build:check` skips packaging.
- `npm test` — vitest. `npm run lint` / `npm run lint:fix` — eslint on `src`.
- `npm run format` — prettier.
- Husky + lint-staged run eslint --fix + prettier on commit.

### WSL execution rules (detect at session start)

If `uname -r` contains `microsoft`/`WSL`, this is WSL — apply the following split:

- `npm run lint` / `npm test`: run directly in WSL bash (pure Node, same result).
- `npm run build:check`: invoke via Windows PowerShell, not WSL bash.
  - Prefer `pwsh.exe` (PowerShell 7); fall back to `powershell.exe` (5.1) if absent.
  - Detect: `command -v pwsh.exe >/dev/null && PS=pwsh.exe || PS=powershell.exe`
  - Invoke: `"$PS" -NoProfile -Command "cd 'D:\project_poe2\POE2-unofficial-launcher'; npm run build:check"`
  - On failure (e.g. native module mismatch), fall back to asking the user to run it on Windows.
- Tasks requiring in-game verification (e.g. font work): a passing build is not sufficient — always request the user's live check.

### WSL execution rules (detect at session start)

If `uname -r` contains `microsoft`/`WSL`, this is WSL — apply the following split:

- `npm run lint` / `npm test`: run directly in WSL bash (pure Node, same result).
- `npm run build:check`: invoke via Windows PowerShell, not WSL bash.
  - Prefer `pwsh.exe` (PowerShell 7); fall back to `powershell.exe` (5.1) if absent.
  - Detect: `command -v pwsh.exe >/dev/null && PS=pwsh.exe || PS=powershell.exe`
  - Invoke: `"$PS" -NoProfile -Command "cd 'D:\project_poe2\POE2-unofficial-launcher'; npm run build:check"`
  - On failure (e.g. native module mismatch), fall back to asking the user to run it on Windows.
- Tasks requiring in-game verification (e.g. font work): a passing build is not sufficient — always request the user's live check.

## Code style

- State assumptions; ask when ambiguous.
- Minimum code that solves the problem. No speculative abstractions.
- Surgical edits — every changed line traces to the task.
- Define a verifiable success check before declaring done.
