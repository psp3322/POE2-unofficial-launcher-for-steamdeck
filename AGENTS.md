# AGENTS.md — POE2 Unofficial Launcher

Electron + React 19 + Vite 8 + TypeScript launcher for Path of Exile / PoE 2.
Layout: `src/main/` (Electron main), `src/renderer/` (React UI), `src/shared/` (shared types).

This file is the Codex entry point for project rules. Keep it aligned with `CLAUDE.md`; do not fork behavior between agents unless the instruction is agent-specific.

## Documentation status

`README.md`, `docs/README_KR.md` were rewritten on 2026-05-13 from the wiki analysis and are current as of that date. They describe user-facing behavior, not architecture. `CHANGELOG.md` is auto-generated by release-please. `.agents/skills/*` predate the wiki and may be stale; verify against source.

The canonical maintained project context is the wiki below. Start there for non-trivial work.

## Project memory

Persistent project context lives at `~/project_llm_wiki/wiki/projects/poe2-launcher.md` (Obsidian vault, Korean). Read it at the start of any non-trivial work.

After a meaningful decision or analysis, dump a raw note to:

`~/project_llm_wiki/raw/projects/poe2-launcher/<YYYY-MM-DD>-<topic>.md`

Then in the wiki repo run:

`/ingest raw/projects/poe2-launcher/<file>.md`

The wiki update follows the INGEST operation in the wiki's `CLAUDE.md` (surgical edits, `[source: raw/...]` citations).

## Commands

- `npm run dev` — Vite dev server (Electron). `chcp 65001` for UTF-8 on Windows.
- `npm run build` — `tsc && vite build && electron-builder`. `npm run build:check` skips packaging.
- `npm test` — vitest.
- `npm run lint` / `npm run lint:fix` — eslint on `src`.
- `npm run format` — prettier.
- Husky + lint-staged run eslint --fix + prettier on commit.

## GitHub CLI token

Before any repo-local `gh` command or GitHub HTTPS push, use the
`github-cli-token` skill at `.agents/skills/github-cli-token/SKILL.md`.
It defines the repo-local `GH_TOKEN` injection pattern and avoids mutating
global GitHub CLI auth state.

## WSL execution rules

Detect WSL at session start. If `uname -r` contains `microsoft` or `WSL`, this is WSL.

WSL is the development primary for editing, git, focused source inspection, and pure Node scripts. Windows is the build/run primary for Electron, npm package scripts, and actual POE / POE2 game tests. Both OSes share the same `node_modules` under `D:\project_poe2\POE2-unofficial-launcher\`.

Split commands by where they belong:

- WSL bash, direct: pure Node scripts only, such as `node -e ...` for hypothesis checks.
- Do not run eslint or vitest in WSL. The shared `node_modules` requires Windows-compatible native binaries/wrappers for this repo.
- Windows PowerShell only: `npm install`, `npm ci`, `npm run build`, `npm run build:check`, `npm run dev`, `npm run lint`, `npm run lint:fix`, `npm test`.

Prefer `pwsh.exe` (PowerShell 7); fall back to `powershell.exe` (5.1) if absent:

```bash
command -v pwsh.exe >/dev/null && PS=pwsh.exe || PS=powershell.exe
"$PS" -NoProfile -Command "cd 'D:\project_poe2\POE2-unofficial-launcher'; npm run <script>"
```

Why `npm install` / `npm ci` must not run in WSL: WSL's npm writes POSIX symlinks under `node_modules/.bin/` and does not create Windows wrappers (`.cmd`, `.ps1`). The next Windows `npm run build` / `npm run dev` can then fail with `'tsc' is not recognized` or `'vite' is not recognized`.

If dependencies need reinstalling (lock conflict, native binding error, pre-commit hook failing on `unrs-resolver`, etc.), ask the user to run `npm ci` from Windows pwsh. Do not auto-repair from WSL.

If a pre-commit hook fails in WSL due to native-binding mismatch, do not try to fix the environment. Either ask the user to commit from Windows, or get explicit approval for `--no-verify` for that single commit.

Tasks requiring in-game verification, such as font work or launcher boot, need a user live check on Windows. A passing build is not sufficient.

## Code style

- State assumptions; ask when ambiguous.
- Minimum code that solves the problem. No speculative abstractions.
- Surgical edits: every changed line must trace to the task.
- Define a verifiable success check before declaring done.
