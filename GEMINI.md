# GEMINI.md — POE2 Unofficial Launcher

All project rules live in **`AGENTS.md`** (single source for all coding
agents). Read `AGENTS.md` in full before doing anything else, and follow it
as if its contents were written here.

Notes for Gemini:

- Claude-specific sections referenced there (`.claude/agents/`, model
  routing in `CLAUDE.md`) do not apply to you — everything else does.
- Skills under `.agents/skills/*/SKILL.md` are plain markdown playbooks:
  read the relevant one before working in its area, as AGENTS.md directs.
